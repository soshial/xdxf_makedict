- fix duplication in rus_bse(Ядерная физика)
- merge old scheme of plugins from 0.2 with 0.3
- dsl_parser: change testing code for file exist
- more informatical error messages in generator.cpp
- check if "make dist" to work
- refactor xml special characters handling, avoid code duplication
- rentrant parser and generator code?
- fix formatation of makedict -l output
- test for this, test for external parser plugin
- test all cases parser<->generator, makedict<->generator etc
- keys <opt> <nu> generator?
- print where we create dictionary
- why t_dsl_parser so slow?
- perfomance test
- generator results, if parser and generator are internal
- split utils.h to resource.hpp and utils.hpp by it self
- "nu" implementation
- check all to do items in code
- superfluous copy when string->StringList
- xdxf_generator: whats happened if abbr list keys contains opt and nu
